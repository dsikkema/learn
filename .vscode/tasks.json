{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Like Dale",
            "type": "shell",
            "command": "dalerun ${fileBasename}",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": []
        }
    ]
}