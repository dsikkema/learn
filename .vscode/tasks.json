{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Executable",
            "type": "shell",
            "command": "${file}",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": []
        }
    ]
}